# -------------------------------------
# Application Environment
# -------------------------------------
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000
API_PREFIX=api

# -------------------------------------
# Database
# This must match the user, password, port, and db name in your docker-compose.yml
# -------------------------------------
DATABASE_URL="postgresql://user:password@localhost:5432/mydatabase?schema=public"

# -------------------------------------
# Security & JWT
# Use a long, random string for JWT_SECRET in production
# -------------------------------------
JWT_SECRET="your-super-secret-and-long-jwt-key"
JWT_EXPIRES_IN=1d

# -------------------------------------
# Default Admin User for Seeding
# These credentials will be used by the 'prisma db seed' command
# -------------------------------------
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=superadminpassword123

# -------------------------------------
# Rate Limiting (Optional - Defaults are set in the code)
# -------------------------------------
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100